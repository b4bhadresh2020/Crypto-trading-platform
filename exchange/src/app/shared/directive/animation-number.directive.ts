// import { Directive, ElementRef, Renderer2, Input, OnInit } from '@angular/core';

// @Directive({
//   selector: '[appAnimationNumber]'
// })

// export class AnimationNumberDirective implements OnInit {
//   @Input() value: string;

//   constructor(private renderer: Renderer2, private el: ElementRef) {
//   }

//   ngOnInit() {
//     this.el.nativeElement.style.animationDelay = 1;
//     this.renderer.setStyle(this.el.nativeElement, 'animation-delay', '1');
//   }
// }
